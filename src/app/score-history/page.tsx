import { PlaceholderPage } from "@/components/layout/PlaceholderPage";

export default function ScoreHistoryPage() {
  return (
    <PlaceholderPage
      activeHref="/score-history"
      description="Lịch sử cộng điểm của bạn sẽ được hiển thị tại đây."
      icon="history"
      title="Lịch sử điểm"
    />
  );
}
